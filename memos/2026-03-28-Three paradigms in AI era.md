- LLM without reasonsing
  logseq.order-list-type:: number
	- A lot of compute for training
	  logseq.order-list-type:: number
- LLM with reasoning - more compute
  logseq.order-list-type:: number
	- More compute is needed to reasoning tokens
	  logseq.order-list-type:: number
	- Reduced hallucinations and increased accuracy drive up usage
	  logseq.order-list-type:: number
	- Post-training needs more compute
	  logseq.order-list-type:: number
- Agents - even exponentionally more compute
  logseq.order-list-type:: number
	- Agents make multiple calls to LLMs to get an answer
	  logseq.order-list-type:: number
	- Tool use is better done by CPUs. CPUs enter the stage.
	  logseq.order-list-type:: number
	- Better usefulness drives up usage further
	  logseq.order-list-type:: number
- ### Agentic AI era
- A critical component of making agentic workloads work is the “harness”, i.e. the software that actually controls the model. Claude Code and OpenAI’s Codex actually abstract the user away from the model: you give instructions to an agent, which actually directs the model; critically, the agent can also use other deterministic tools as well, which means that it can verify its results
- Large language models are intelligent, but they do not have goals or values or drive. They are tools to be used by, well, anyone who is willing and able to take the initiative to use them.
- the positive impact of AI will not be in *eliminating* jobs, but rather *replacing* hard-to-manage-and-motivate human cogs in the organizational machine with agents that not only do what they are told but do so tirelessly and continuously until the job is done.
- ### Why are we NOT in an AI bubble?
	- All of the weaknesses of LLMs are being addressed by exponential increases in compute.
	- The number of people who need to wield AI effectively for demand to skyrocket is decreasing
	- The economic returns from using agents aren’t just impactful on the bottom line, but the top line as well.
- ### Where does the value go?
- profits flow away from modular parts of the value chain — which are commoditized — and flow towards integrated parts of the value chain, which are differentiated
- model performance isn’t the only thing that matters: the integration between model and harness is where true agent differentiation is found.
- if agents require integration between model and harness, that the companies building that integration — specifically Anthropic and OpenAI (Gemini is a strong model, but Google hasn’t yet shipped a compelling harness) — are actually poised to be significantly more profitable than it might have seemed as recently as late last year.
- Microsoft Copilot Cowork — unlike the Copilot chatbot — is not model agnostic: Cowork is an agent, which means it needs both a model and a harness, and those are two integrated pieces, not modular components.